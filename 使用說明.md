# WhisperTranscriber 使用說明

將音訊 / 視訊自動轉為 SRT 字幕，支援翻譯

---

## 第一次使用

1. 雙擊 `setup.bat`，等待安裝完成（需要網路）
   - 自動安裝 Python、.NET 執行環境與所需套件
   - 出現「安裝完成！」即可關閉視窗

2. 雙擊 `WhisperGUI.exe` 開啟程式

3. 第一次按「開始轉錄」時，程式會自動下載 AI 模型（約 3 GB）
   下載期間請耐心等候，視窗狀態列會顯示已等待秒數

---

## 基本使用步驟

1. 將音訊或視訊檔案拖放到程式視窗（可一次拖入多個），或點「新增檔案」選擇

2. 確認語言設定：
   - 日文日劇 / ASMR → 選「ja（日文）」
   - 其他語言 → 選對應語言或「auto（自動偵測）」

3. 點「開始轉錄」

4. 佇列中每個檔案的狀態會即時更新（等待中 → 轉錄中 → 翻譯中 → 完成）

5. 完成後自動產生 SRT 字幕檔（與音訊放在同一資料夾）

---

## 批次佇列

可一次加入多個檔案，程式會依序處理：

- **新增檔案**：點「新增檔案」按鈕或直接拖放多個檔案
- **移除**：選取後點「移除」
- **清除完成**：點「清除完成」移除已完成項目
- 翻譯啟用時，程式自動使用 Pipeline 模式：上一筆翻譯與下一筆轉錄同時進行

勾選「完成時通知」可在全部處理完後顯示 Windows 通知。

---

## 翻譯功能

勾選「翻譯」即可在轉錄後自動翻譯字幕。

輸出兩個檔案：
- `input.srt` — 純譯文（直接掛載觀賞用）
- `input_review.srt` — 原文 + 譯文對照（用 Subtitle Edit 校對用）

### 翻譯後端選擇

| 後端 | 費用 | 說明 |
|---|---|---|
| googletrans（預設） | 免費 | 不需申請，速度快，品質普通 |
| Gemini（免費額度） | 免費額度 | 需申請 API Key，品質佳；模型可在進階設定中更換 |
| Claude Haiku API | 付費 | 需申請 API Key，穩定 |
| Claude Sonnet API | 付費 | 需申請 API Key，品質最佳 |
| Claude CLI | 需訂閱 | 需安裝 Claude 桌面版 |

---

## 如何取得 API Key

### Gemini API Key（免費額度，推薦）

1. 前往 [Google AI Studio](https://aistudio.google.com/)，登入 Google 帳號
2. 點左側「Get API key」→「Create API key」
3. 複製 Key（以 `AIza` 開頭），貼到程式的 Key 欄位

### Anthropic API Key（Claude Haiku / Sonnet）

1. 前往 [Anthropic Console](https://console.anthropic.com/)，註冊或登入
2. 點右上角「API Keys」→「Create Key」
3. 複製 Key（以 `sk-ant-` 開頭），貼到程式的 Key 欄位

---

## ASMR 雙聲道分離

Binaural ASMR（左右耳不同聲音）可分別轉錄：

「聲道」選「分離聲道」→ 開始轉錄

輸出的 SRT 中每條字幕會標示 `[L]` 左耳 或 `[R]` 右耳

---

## 常見問題

**Q: 第一次轉錄等很久，是當掉了嗎？**
A: 不是。首次執行需下載約 3 GB 的 AI 模型，請耐心等候。狀態列顯示「載入模型中… XX 秒」表示正在運作。

**Q: 出現「找不到 venv/Scripts/python.exe」**
A: 請先執行 `setup.bat` 安裝環境。

**Q: 翻譯結果有幾段是亂碼或原文**
A: 翻譯失敗的段落會自動改用 googletrans 補翻，不影響整體。

**Q: 出現「找不到檔案」錯誤**
A: 程式在載入模型前會先確認所有檔案存在。請確認路徑正確，或重新拖放檔案。

**Q: 出現「顯示卡記憶體不足」**
A: 關閉其他使用 GPU 的程式（遊戲、其他 AI 工具），再重試。或改用較小的模型（medium / small）。

**Q: GPU 無法使用**
A: 在「裝置」選「cpu」、「精度」選「int8」可改用 CPU 執行。速度較慢，約為 GPU 的 1/10。

**Q: 字幕時間軸跑掉**
A: 嘗試關閉「合併相似字幕段落」，或啟用「VAD 靜音過濾」。

---

如有問題，請至 [GitHub Issues](https://github.com/jeremywsm/WhisperTranscriber/issues) 回報
